import 'package:flutter/material.dart';

class AppColors {
  // Core Surface & Backgrounds
  static const Color background = Color(0xFFF4F7FA);
  static const Color backgroundGrey = Color(0xFFEEF2F6); // Cool operational surface
  static const Color cardBg = Colors.white;

  // Typography
  static const Color textMain = Color(0xFF142235);
  static const Color textMuted = Color(0xFF718096);
  static const Color textSoft = Color(0xFF526176);

  // Brand Colors (Matched to Login Screen)
  static const Color primary = Color(0xFF123B5D); // Navy
  static const Color primarySoft = Color(0x1A167C80); // Light teal tint
  static const Color primarySoftBg = Color(0xFFE3F3F2); // Light teal background
  static const Color accent = Color(0xFF167C80); // Teal

  // Status Badges
  static const Color statusTransit = Color(0xFFE0F7FA);
  static const Color statusTransitText = Color(0xFF00838F);
  static const Color statusProcess = Color(0xFFFFF8E1);
  static const Color statusProcessText = Color(0xFFFFB300);
  static const Color statusDelivered = Color(0xFFE6F9F0);
  static const Color statusDeliveredText = Color(0xFF10B981);
  static const Color statusDanger = Color(0xFFFEE2E2);
  static const Color statusDangerText = Color(0xFFEF4444);
}
