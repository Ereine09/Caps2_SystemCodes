# Rider Flutter App (Dart)

This app will connect to the existing PHP backend for rider assignments, status updates, QR confirmation, and chat.

## Next steps
1. Create the real Flutter project files in this folder.
2. Implement API client + screens.
3. Add rider backend API endpoints (separate from admin/staff UI files).

## Configuration
- Backend base URL should be provided in a build-time or runtime config.

